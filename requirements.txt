# ARGUS Dashboard — Deployment Dependencies (Streamlit Community Cloud)
# Only what src/dashboard/app.py and its imports actually need at runtime.
# NO torch / faiss / scikit-learn / faker — dashboard is static-data only.

streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0
altair>=5.0.0
matplotlib>=3.7.0
networkx>=3.1
scipy>=1.10.0
